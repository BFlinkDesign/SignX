# Authentication & Security - Phase 1 Additions
pyotp>=2.9.0              # TOTP (Time-based One-Time Password) for 2FA
qrcode>=7.4.2             # QR code generation for authenticator apps
Pillow>=10.0.0            # Required by qrcode for image generation
twilio>=8.10.0            # SMS provider for MFA
sendgrid>=6.11.0          # Email provider for MFA
aiohttp>=3.9.0            # Async HTTP client for OAuth2 flows
PyJWT>=2.8.0              # JWT handling for Apple Sign In (ES256)
cryptography>=41.0.0      # Cryptographic primitives for JWT signing

# Add these to your existing requirements-dev.txt or install separately:
# pip install pyotp qrcode Pillow twilio sendgrid aiohttp PyJWT cryptography
